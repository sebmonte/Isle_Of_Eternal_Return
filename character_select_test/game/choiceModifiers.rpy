init python:
    class choiceModifiers:
        def __init__(self, name, description, modifier):
            self.name = name
            self.descroption = description
            self.modifier = modifier
